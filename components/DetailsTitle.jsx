import styles from "./DetailsTitle.module.css";

const DetailsTitle = ({ }) => {
  return (
    <div className={styles.container}>
      <h3>Details</h3>
    </div>
  );
};

export default DetailsTitle;